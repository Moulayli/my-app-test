import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter976Ioc',
  appName: 'MyAppTest',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
